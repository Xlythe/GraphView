# Pin the SDK the tests run against. Left to itself Robolectric picks the target SDK, which is now
# new enough to demand Java 21, so the tests would only run for people on the newest JDK. Nothing
# here is version specific, so an older sandbox tests it just as well.
sdk=35
